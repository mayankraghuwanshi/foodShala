


const initialState = {
    isLoading : false,
    restaurants:[],
    restaurant:{}
}

export const restaurantReducer =(state = initialState , {type , payload})=>{
        switch (type) {
            default : return state;
        }
}